# build output
bin/
obj/
publish/

# CI (and any local reproduction of it) checks the editor repo out here for the schema-parity guard — a whole
# second repository that must never be committed into this one. See .github/workflows/ci.yml.
_enc/

# local game DLLs (Unity + Amplitude — not redistributable; copy them in per README)
References/

# IDE / user
*.user
.vs/
.idea/
*.suo

# third-party model binaries (download per CC-BY)
*.fbx
*.glb
*.gltf
*.usdz


# Blender bytecode cache
editor/Tools~/__pycache__/
__pycache__/
